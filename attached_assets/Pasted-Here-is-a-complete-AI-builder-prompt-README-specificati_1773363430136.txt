Here is a **complete AI builder prompt / README specification** you can give to an AI coding agent (Cursor, Devin-style agents, Replit AI, etc.) so it builds the **entire production-ready SaaS platform** for you. The text is written like a **professional GitHub README + system prompt for an AI developer**. --- # BallotWave — AI Build Specification / README ## Project Name **BallotWave** ## Tagline **Next-Generation Digital Voting Infrastructure for Schools in Africa** BallotWave is a **secure SaaS election platform** designed for **Senior High Schools, Colleges, and Universities** to conduct **digital elections using Web + USSD voting with Mobile Money payments**. The platform supports **department-level elections, role-based access control, fraud detection, analytics dashboards, and monetization via platform commission.** BallotWave will integrate with: * **Paystack** for Mobile Money payments * **Arkesel** for USSD voting and SMS OTP verification The system must be **production-ready, scalable, secure, and multi-tenant** so multiple schools can run elections simultaneously. --- # Objective Build a **production-grade SaaS voting system** that enables: * secure elections * USSD voting * web voting * payment-based voting * role-based dashboards * fraud detection * monetization The platform must support **thousands to millions of votes safely.** --- # Core Product Vision BallotWave should feel like a mix of: * **Stripe (payments infrastructure)** * **Election Commission system** * **Campus election platform** * **Analytics dashboard** The platform must be designed to **scale across Africa for schools, universities, unions, and organizations.** --- # Core Technology Stack ## Frontend * Next.js 14 * TypeScript * TailwindCSS * React Query * Shadcn UI Features: * responsive UI * mobile friendly * SEO optimized * real-time updates --- ## Backend * Node.js * Express.js * TypeScript * PostgreSQL * Redis * BullMQ (queues) Architecture: * REST API * modular services * scalable architecture --- ## Infrastructure Hosting: Frontend * Vercel Backend * AWS / Railway / Render Database * PostgreSQL Cache * Redis Security * Cloudflare --- # Key Integrations ## Payment Gateway Use **Paystack** Payment methods: * MTN Mobile Money * Vodafone Cash * AirtelTigo Money Payment Flow: 1 User clicks vote 2 User selects candidate 3 User pays vote fee via Paystack 4 Payment verified via webhook 5 Vote recorded --- ## USSD Integration Use **Arkesel USSD API** USSD example: ``` *789*2027# ``` Menu: ``` 1 Vote 2 View Candidates 3 Election Results ``` Vote flow: ``` Select Position 1 President 2 Secretary Select Candidate 1 John 2 Mary ``` System receives: * phone number * candidate id * election id Then records vote. --- ## SMS OTP Use **Arkesel SMS API** For: * voter verification * login verification * vote confirmation --- # Core Platform Features ## Multi-School SaaS System The system must support **multiple schools**. Structure: ``` Platform School Faculty Department Election Position Candidate ``` Example: ``` KSTU Faculty of Engineering Computer Science Department SRC Election President ``` --- # User Roles ## Super Admin (Platform Owner) Full control over BallotWave. Permissions: * create schools * manage commissions * monitor elections * view revenue * freeze elections * manage payouts Dashboard metrics: * total votes * revenue * active elections * fraud alerts --- ## School Admin Controls elections inside a school. Permissions: * create elections * add departments * approve candidates * manage voters * view analytics --- ## Election Officer Manages specific election. Permissions: * manage candidates * monitor voting * manage results --- ## Candidate Student contesting in election. Features: * candidate profile * manifesto * campaign media * vote statistics --- ## Voter Student voter. Capabilities: * register * verify student ID * vote * view candidates --- # Voting Methods ## Web Voting Flow: ``` login verify phone select election select position select candidate pay vote fee vote confirmed ``` Restrictions: * one vote per position * OTP verification * device tracking --- ## USSD Voting Flow: ``` Dial USSD Select election Select position Select candidate Confirm vote ``` USSD server will call backend API. --- # Fraud Prevention BallotWave must include: ### Device fingerprinting Detect: * multiple votes * bot activity Use: FingerprintJS --- ### Rate limiting Prevent: * vote flooding * bot attacks --- ### OTP Verification All voters must verify phone. --- ### Voting anomaly detection Detect: * sudden vote spikes * unusual patterns --- # Monetization System BallotWave must include **platform commission settings**. Example: ``` Vote fee = 1 GHS Platform commission = 10% School receives = 90% ``` Admin can configure: * vote fee * commission percentage --- # Revenue Dashboard Super Admin dashboard must display: * total revenue * platform commission * school payouts * election statistics --- # Payout System Schools must be able to withdraw funds. Flow: ``` school requests payout platform reviews funds sent via Paystack transfer ``` --- # Public Election Pages Each election must have a **public page**. Example: ``` ballotwave.com/kstu/src-election ``` Displays: * candidates * manifesto * election countdown * voter turnout --- # Live Results Dashboard Admins can view: * votes per minute * votes by department * candidate ranking --- # Analytics System Charts: * voter turnout * vote distribution * department participation * candidate popularity --- # Notifications Use: * SMS * email Notifications for: * vote confirmation * election start * election results --- # Security Requirements The system must include: * HTTPS * encrypted votes * audit logs * rate limiting * SQL injection protection * CSRF protection --- # Database Schema (Core Tables) Users ``` id name email phone password role school_id ``` Schools ``` id name domain created_at ``` Departments ``` id school_id name ``` Elections ``` id school_id department_id title start_date end_date status ``` Positions ``` id election_id title ``` Candidates ``` id user_id position_id manifesto ``` Votes ``` id user_id candidate_id election_id device_hash created_at ``` Payments ``` id user_id amount payment_reference status ``` --- # API Structure ``` /auth /users /schools /departments /elections /candidates /votes /payments /analytics /ussd ``` --- # Required Admin Dashboards Super Admin * revenue analytics * election overview * fraud monitoring * payouts School Admin * election creation * candidate management * vote tracking --- # Performance Requirements System must handle: * 100k+ voters * high concurrency * fast vote recording Use: * Redis caching * background queues --- # Developer Requirements Code must be: * modular * well documented * production ready * scalable Include: * environment configuration * Docker setup * deployment instructions --- # Deployment The system must include: ``` Dockerfile docker-compose.yml .env.example CI/CD workflow ``` --- # Future Features Design system so it can later support: * blockchain vote verification * mobile apps * AI fraud detection * biometric voting --- # End Goal BallotWave should become **Africa’s leading digital election infrastructure** for: * schools * universities * unions * organizations --- 